# Production (Vercel): set in Project → Settings → Environment Variables
# VITE_API_URL=https://your-rentflow-api.onrender.com

# Leave unset for local `npm run dev` (Vite proxies /api and /uploads to localhost:5000)
